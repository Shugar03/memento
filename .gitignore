# Rust build artifacts
/target/

# Local data: per-tenant stores, backups, downloaded models
/db/
/backups/
/models/

# Environment / secrets
.env

# Editor / OS cruft
*.swp

# JS tooling (anydoc invoked via npx)
node_modules/
dist/

# Test / coverage artifacts
coverage/
*.log

# Local agent tooling (skill registry, opencode config) — not part of
# the project; regenerated per-machine by `gentle-ai skill-registry refresh`
# and `opencode` itself.
.atl/
.opencode/

# NOTE: Cargo.lock IS committed on purpose — this workspace ships binaries
# (CLI, MCP server, worker) and locked builds are required.

# User's private architecture manifesto (not for public repo)
manifesto memoria.md
