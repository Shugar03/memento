//! Per-binary tracing subscribers on stderr (REQ-OBS-001/002, design D4).
//! Implemented via strict TDD: the module's gate + format resolution tests
//! land here first (RED), then the subscribers themselves.
